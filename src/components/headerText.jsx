import React from 'react';

const HeaderText = () => {
  return (<div className="text-center form-floating">
    <h3>
      <small className="text-muted">When the image </small><br />
      is the right way up<br />
      <small className="text-muted"> click Done!</small>
    </h3>
  </div>);
}

export default HeaderText;